{
  "name": "chromium-sidecar",
  "version": "0.4.0",
  "description": "Local agent sidecar for authenticated Chromium browsers, with a Codex plugin and optional Arc Spaces support.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "chromium-sidecar": "./native-host/src/cli.mjs"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "install-host": "node native-host/src/install.mjs",
    "uninstall-host": "node native-host/src/install.mjs --uninstall",
    "sidecar": "node native-host/src/cli.mjs",
    "test": "npm --prefix native-host test && npm --prefix plugins/chromium-sidecar test && node scripts/setup.mjs --dry-run --no-codex --no-open",
    "package:extension": "node scripts/package-extension.mjs"
  }
}
