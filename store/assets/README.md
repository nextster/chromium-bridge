# Store Assets

No visual asset is approved yet. Do not use generated placeholders in a Store submission.

Before packaging a Store build, add approved 16, 32, 48, and 128 pixel PNG icons under `extension/icons`. Add the approved 440x280 promotional image here as `promo-small-440x280.png`.

A real 1280x800 or 640x400 product screenshot must still be captured from the final Store-ID build after popup consent. Do not replace it with a fabricated UI mockup.
